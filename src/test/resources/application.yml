server:
  error:
    include-message: always
  port: 3002

spring:
  profiles:
    active: test

#to include all our endpoints in the actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"





